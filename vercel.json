{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma migrate deploy || echo 'migrate deploy failed - continuing build; resolve migration history separately'; next build"
}
